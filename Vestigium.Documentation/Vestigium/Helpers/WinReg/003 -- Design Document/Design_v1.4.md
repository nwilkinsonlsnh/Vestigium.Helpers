# WinReg — Design 1.4 (as built)

Client + native. Journal JSONL header + muts. Compact rewrites via `.tmp` + replace. Compare walks two indexes with progress. ACL via `RegGetKeySecurity` / `RegSetKeySecurity`.
