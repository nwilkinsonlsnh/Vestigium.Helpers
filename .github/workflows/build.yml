name: build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: Vestigium.Helpers
      - name: Checkout Vestigium.Logging
        uses: actions/checkout@v4
        with:
          repository: nwilkinsonlsnh/Vestigium.Logging
          path: Vestigium.Logging
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      - name: Restore
        working-directory: Vestigium.Helpers
        run: dotnet restore Vestigium.Helpers.slnx
      - name: Build
        working-directory: Vestigium.Helpers
        run: dotnet build Vestigium.Helpers.slnx -c Release --no-restore
      - name: Test
        working-directory: Vestigium.Helpers
        run: dotnet test src/Vestigium.Helpers.Tests/Vestigium.Helpers.Tests.csproj -c Release --no-build --logger "trx;LogFileName=test-results.trx"
