namespace Vestigium.Helpers.Json;

/// <summary>
/// System.Text.Json helpers for file and stream payloads. Skeleton surface — behaviour is specified in this project's SRS.
/// </summary>
public static class JsonHelper
{
    /// <summary>
    /// Returns the assembly identity so hosts and tests can prove the library loaded.
    /// </summary>
    public static string Identity => "Vestigium.Helpers.Json";
}
