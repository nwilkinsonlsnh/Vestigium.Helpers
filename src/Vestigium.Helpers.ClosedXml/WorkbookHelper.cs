namespace Vestigium.Helpers.ClosedXml;

/// <summary>
/// ClosedXML wrappers for reading and writing Excel workbooks. Skeleton surface — behaviour is specified in this project's SRS.
/// </summary>
public static class WorkbookHelper
{
    /// <summary>
    /// Returns the assembly identity so hosts and tests can prove the library loaded.
    /// </summary>
    public static string Identity => "Vestigium.Helpers.ClosedXml";
}
