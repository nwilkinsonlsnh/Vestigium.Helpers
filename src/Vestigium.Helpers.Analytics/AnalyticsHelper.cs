namespace Vestigium.Helpers.Analytics;

/// <summary>
/// Lightweight counters, timings, and metric snapshots. Skeleton surface — behaviour is specified in this project's SRS.
/// </summary>
public static class AnalyticsHelper
{
    /// <summary>
    /// Returns the assembly identity so hosts and tests can prove the library loaded.
    /// </summary>
    public static string Identity => "Vestigium.Helpers.Analytics";
}
