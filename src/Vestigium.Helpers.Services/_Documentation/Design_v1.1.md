# Services — Design 1.1 (as built)

Snapshotter lists SCM + hidden. Tree walker DFS with MaxDepth 16 and MaxNodes 256 unique names. Control uses ServiceController + native config for logon / recovery.
